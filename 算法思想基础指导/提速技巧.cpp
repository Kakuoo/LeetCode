#include <iostream>
using namespace std;

auto speedup = []() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    return 0;
}();

static int x = []() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    return 0;
}();

static const auto _____ = []() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    return 0;
}();